# Turbo 构建系统详解

## 什么是 Turbo？

**Turbo (Turborepo)** 是一个为 Monorepo 设计的高性能构建系统。

### 核心功能

- **智能调度** - 自动分析依赖关系，按正确顺序构建
- **并行执行** - 充分利用 CPU，并行构建无依赖的包
- **智能缓存** - 缓存构建结果，只重新构建修改的包
- **增量构建** - 检测文件变化，只构建受影响的包
- **远程缓存** - 团队共享构建缓存，加速 CI/CD

## 为什么需要 Turbo？

### 问题 1：手动构建太麻烦

假设项目有这样的依赖关系：

```
util (无依赖)
 ↓
core (依赖 util)
 ↓
plugin (依赖 core)
 ↓
cli (依赖 plugin)
```

#### 没有 Turbo：

```bash
# 必须手动按顺序构建
cd packages/util
bun run build        # 1. 先构建 util (10秒)

cd ../core
bun run build        # 2. 再构建 core (15秒)

cd ../plugin
bun run build        # 3. 再构建 plugin (20秒)

cd ../cli
bun run build        # 4. 最后构建 cli (5秒)

# 😫 总时间：50 秒
# 😫 容易出错（忘记某个包）
# 😫 必须记住顺序
```

#### 有了 Turbo：

```bash
# 在根目录运行一条命令
bun turbo build

# Turbo 自动：
# ✅ 分析依赖关系
# ✅ 按正确顺序构建
# ✅ 显示进度
# ✅ 缓存结果

# 总时间：50 秒（第一次）
# 总时间：0.5 秒（第二次，从缓存）⚡
```

### 问题 2：全量构建浪费时间

```bash
# 场景：只修改了 cli/src/index.ts

# 没有 Turbo：
bun run build
# ❌ 重新构建所有包
# ⏱️  util: 10秒
# ⏱️  core: 15秒
# ⏱️  plugin: 20秒
# ⏱️  cli: 5秒
# 总计：50 秒

# 有了 Turbo：
bun turbo build
# ✅ 从缓存恢复 util, core, plugin
# ✅ 只重新构建 cli
# ⏱️  cli: 5秒
# 总计：5 秒 (快 10 倍！)
```

### 问题 3：串行执行效率低

假设项目结构：

```
       core
      ↙    ↘
  plugin   util   (两者都依赖 core，但互不依赖)
      ↘    ↙
       cli
```

#### 没有 Turbo（串行）：

```bash
core (10秒) → plugin (15秒) → util (12秒) → cli (20秒)
总时间：57 秒
```

#### 有了 Turbo（并行）：

```bash
core (10秒)
    ↓
┌───┴────┐
plugin   util  (并行执行！15秒和12秒同时进行)
└───┬────┘
    ↓
cli (20秒)

总时间：10 + max(15, 12) + 20 = 45 秒 (节省 12 秒！)
```

## Turbo 配置文件

### turbo.json

```json
{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "typecheck": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    }
  }
}
```

### 配置详解

#### 1. `dependsOn: ["^build"]`

```json
"build": {
  "dependsOn": ["^build"]
}
```

**含义：**
- `^` 表示"依赖的包的"
- `build` 表示"build 任务"
- 完整意思："在构建当前包之前，先构建所有依赖的包"

**实例：**
```
cli 依赖 plugin
plugin 依赖 core

运行 turbo build：
1. ✅ 先构建 core（无依赖）
2. ✅ 再构建 plugin（依赖 core）
3. ✅ 最后构建 cli（依赖 plugin）
```

#### 2. `outputs: ["dist/**"]`

```json
"build": {
  "outputs": ["dist/**"]
}
```

**含义：**
- 告诉 Turbo 构建产物在 `dist/` 目录
- Turbo 会缓存这个目录
- 下次构建时，如果源码没变，直接从缓存恢复

**效果：**
```bash
# 第一次构建
bun turbo build
>>> core:build    ✓ (15s)
>>> Cached: dist/

# 第二次构建（无修改）
bun turbo build
>>> core:build    ✓ (0.1s) [CACHE] ⚡
```

#### 3. `cache: false`

```json
"dev": {
  "cache": false
}
```

**含义：**
- 禁用缓存（每次都重新执行）
- 适用于开发服务器等不应缓存的任务

#### 4. `persistent: true`

```json
"dev": {
  "persistent": true
}
```

**含义：**
- 任务会持续运行（不会退出）
- 适用于开发服务器、watch 模式

#### 5. 特定包的任务

```json
"cli#test": {
  "dependsOn": ["^build"]
}
```

**含义：**
- 只在 `cli` 包中运行 `test` 命令
- `#` 语法指定特定包

## 实际使用场景

### 场景 1：构建所有包

```bash
bun turbo build

# Turbo 执行流程：
┌─────────────────────────────────────────────┐
│ Step 1: 分析依赖图                           │
├─────────────────────────────────────────────┤
│ util (无依赖)                                │
│  ↓                                          │
│ core (依赖 util)                            │
│  ↓                                          │
│ plugin (依赖 core)                          │
│  ↓                                          │
│ cli (依赖 plugin)                           │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Step 2: 按拓扑顺序构建                        │
├─────────────────────────────────────────────┤
│ >>> util:build       ✓ (10s)               │
│ >>> core:build       ✓ (15s)               │
│ >>> plugin:build     ✓ (20s)               │
│ >>> cli:build        ✓ (5s)                │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Step 3: 缓存结果                             │
├─────────────────────────────────────────────┤
│ ✓ Cached: util, core, plugin, cli          │
│ 📦 Total: 4 packages                        │
│ ⏱️  Time: 50s                               │
└─────────────────────────────────────────────┘
```

### 场景 2：类型检查所有包

```bash
bun turbo typecheck

# Turbo 并行执行：
>>> util:typecheck     ✓ (1.2s)
>>> core:typecheck     ✓ (1.8s)  ⎤
>>> plugin:typecheck   ✓ (1.5s)  ⎥ 同时进行！
>>> cli:typecheck      ✓ (1.0s)  ⎦

📦 4 packages
⏱️  1.8s (最长的那个)
```

### 场景 3：增量构建

```bash
# 修改了 plugin/src/tool.ts
vim packages/plugin/src/tool.ts

bun turbo build

# Turbo 智能检测：
>>> util:build         ✓ (0.1s) [CACHE] ⚡
>>> core:build         ✓ (0.1s) [CACHE] ⚡
>>> plugin:build       ✓ (20s)  [REBUILT] 🔨
>>> cli:build          ✓ (5s)   [REBUILT] 🔨

# ✅ util 和 core 从缓存恢复
# ✅ plugin 重新构建（因为修改了）
# ✅ cli 重新构建（因为依赖 plugin）
# ⏱️  总时间：25.2 秒（而不是 50 秒）
```

### 场景 4：并行开发

```bash
bun turbo dev

# Turbo 并行启动所有开发服务器：
>>> cli:dev            Running on http://localhost:3000
>>> web:dev            Running on http://localhost:5173
>>> docs:dev           Running on http://localhost:4321

# ✅ 三个服务器同时运行
# ✅ 输出分离，易于调试
```

## Turbo 的缓存机制

### 缓存原理

Turbo 通过以下信息计算缓存键：

1. **源码内容** - 文件的哈希值
2. **依赖版本** - package.json 的依赖
3. **环境变量** - 影响构建的环境变量
4. **任务配置** - turbo.json 的配置

```bash
# 缓存键计算
Hash = SHA256(
  源码文件内容 +
  package.json +
  turbo.json +
  环境变量
)

# 如果缓存键相同，直接使用缓存
# 如果缓存键不同，重新构建
```

### 缓存存储位置

```
node_modules/.cache/turbo/
├── 7a8f9b3c2d1e.tar.gz    # 缓存 1
├── 4d5e6f7a8b9c.tar.gz    # 缓存 2
└── 1b2c3d4e5f6a.tar.gz    # 缓存 3

# 每个缓存包含：
# - dist/ 目录（构建产物）
# - 日志文件
# - 元数据
```

### 缓存命中示例

```bash
# 第一次构建（无缓存）
bun turbo build
>>> core:build    ✓ (15s)
>>> Caching...    dist/

# 第二次构建（缓存命中）
bun turbo build
>>> core:build    ✓ (0.1s) [CACHE HIT] ⚡
>>> Restoring...  dist/

# 速度提升 150 倍！
```

## 根 package.json 的脚本配置

```json
{
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "2.5.6"
  }
}
```

### 使用方式

```bash
# 在根目录运行
bun run build        # 构建所有包
bun run typecheck    # 类型检查所有包
bun run test         # 测试所有包
bun run dev          # 启动所有开发服务器
```

## 高级功能

### 1. 远程缓存（团队协作）

```bash
# 配置远程缓存（Vercel）
npx turbo login
npx turbo link

# 开发者 A 构建并上传缓存
bun turbo build
>>> Uploading cache to remote...

# 开发者 B 拉取代码
git pull
bun turbo build
>>> Downloading cache from remote... ⚡
>>> All tasks cached! (0.3s)

# ✅ 开发者 B 无需重新构建
# ✅ 节省大量时间
# ✅ 特别适合 CI/CD
```

### 2. 过滤器（Filter）

```bash
# 只构建某个包
bun turbo build --filter=cli

# 构建某个包及其依赖
bun turbo build --filter=cli...

# 构建某个包及其依赖者
bun turbo build --filter=...cli

# 构建多个包
bun turbo build --filter={cli,web}
```

### 3. 强制重新构建

```bash
# 忽略缓存，强制重新构建
bun turbo build --force

# 清除所有缓存
rm -rf node_modules/.cache/turbo
```

### 4. 查看执行计划

```bash
# 不执行，只显示执行计划
bun turbo build --dry-run

# 输出：
Tasks to Run:
  util:build
    ↓
  core:build
    ↓
  plugin:build
    ↓
  cli:build
```

### 5. 输出详细日志

```bash
# 显示详细日志
bun turbo build --verbose

# 输出：
[util:build] tsc --noEmit
[util:build] ✓ No errors found
[core:build] tsc --noEmit
[core:build] ✓ No errors found
```

## 性能对比

### 场景：4 个包，冷启动

| 方式 | 第一次构建 | 第二次构建（无修改） | 修改一个包 |
|------|-----------|-----------------|---------|
| **手动** | 50 秒 | 50 秒 | 50 秒 |
| **Turbo** | 50 秒 | 0.5 秒 ⚡ | 5-10 秒 |
| **提升** | 1x | 100x | 5-10x |

### 场景：10 个包，并行构建

| 方式 | 串行执行 | 并行执行 |
|------|---------|---------|
| **手动** | 150 秒 | 需手动管理 |
| **Turbo** | - | 60 秒 ⚡ |
| **提升** | - | 2.5x |

## 最佳实践

### 1. 合理配置 outputs

```json
{
  "build": {
    "outputs": [
      "dist/**",      // 构建产物
      ".next/**",     // Next.js
      "coverage/**"   // 测试覆盖率
    ]
  }
}
```

### 2. 开发任务禁用缓存

```json
{
  "dev": {
    "cache": false,
    "persistent": true
  }
}
```

### 3. 使用环境变量

```json
{
  "build": {
    "env": ["NODE_ENV", "API_URL"],  // 影响构建的环境变量
    "outputs": ["dist/**"]
  }
}
```

### 4. 依赖关系清晰

```json
{
  "test": {
    "dependsOn": ["^build"],  // 测试前先构建依赖
    "outputs": []
  }
}
```

## 与其他工具对比

| 工具 | 适用场景 | 缓存 | 并行 | 依赖感知 |
|------|---------|-----|-----|---------|
| **Turbo** | Monorepo | ✅ | ✅ | ✅ |
| **Nx** | 企业级 Monorepo | ✅ | ✅ | ✅ |
| **Lerna** | Monorepo（传统） | ❌ | ✅ | ✅ |
| **npm workspaces** | 基础 Monorepo | ❌ | ❌ | ✅ |
| **Make** | 通用构建 | 部分 | ✅ | 手动 |

## 总结

### Turbo 的核心优势

1. **自动化** - 无需手动管理构建顺序
2. **智能缓存** - 只重新构建修改的包
3. **并行执行** - 充分利用 CPU
4. **依赖感知** - 自动处理依赖关系
5. **团队协作** - 远程缓存共享

### 为什么是"全局工具"

```json
// 根 package.json
{
  "devDependencies": {
    "turbo": "2.5.6"  // 安装在根目录
  }
}
```

**原因：**
- ✅ 所有包共享同一个 Turbo
- ✅ 统一构建配置（turbo.json）
- ✅ 全局缓存管理
- ✅ 避免版本冲突

### 适用场景

Turbo 适合：
- ✅ 多包 Monorepo 项目
- ✅ 需要频繁构建的项目
- ✅ 有复杂依赖关系的项目
- ✅ 团队协作开发
- ✅ CI/CD 流水线

**在 Monorepo 中，Turbo 是必不可少的构建工具！** 🚀
