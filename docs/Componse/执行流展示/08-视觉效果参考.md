# 视觉效果参考

## 1. 完整执行流程示例

### 1.1 开始执行（思考收起）

```
╭──────────────────────────────────────────────────────────────╮
│                                                              │
│  ⠋ Thinking... (esc to interrupt · 5s · ↓ 128 tokens)       │
│    └ Tip: Press ctrl+t to show thinking process             │
│                                                              │
╰──────────────────────────────────────────────────────────────╯
```

### 1.2 工具调用中

```
╭──────────────────────────────────────────────────────────────╮
│ ⠙ Read(src/context/theme.tsx)                                │
╰──────────────────────────────────────────────────────────────╯

⠹ Analyzing... (esc to interrupt · 12s · ↓ 256 tokens)
  └ Tip: Press esc to interrupt
```

### 1.3 多个工具调用完成

```
╭──────────────────────────────────────────────────────────────╮
│ ● Read(src/context/theme.tsx)                                │
│   └ Read 231 lines from src/context/theme.tsx               │
│                                                              │
│ ● Glob(**/*.tsx)                                             │
│   └ Found 26 files matching pattern                          │
│                                                              │
│ ● Grep(useTheme)                                             │
│   └ Found 18 matches                                         │
│                                                              │
│ ⠼ Edit(src/app.tsx)                                          │
│   └ Editing file...                                          │
╰──────────────────────────────────────────────────────────────╯

⠴ Processing... (esc to interrupt · 45s · ↓ 1024 tokens)
```

### 1.4 展开思考（按 ctrl+t）

```
▼ Thinking
│ Let me analyze the codebase structure first. I notice
│ the project uses a monorepo architecture with packages/
│ core and packages/cli. The theme system is well designed
│ with support for multiple themes. I need to find all
│ files that use the useTheme hook and understand the
│ current color system...
└────────────────────────────────────────────────────────────

╭──────────────────────────────────────────────────────────────╮
│ ● Read(src/context/theme.tsx)                                │
│   └ Read 231 lines from src/context/theme.tsx               │
│                                                              │
│ ● Glob(**/*.tsx)                                             │
│   └ Found 26 files matching pattern                          │
╰──────────────────────────────────────────────────────────────╯

⠦ Reasoning... (esc to interrupt · 58s · ↓ 1536 tokens)
```

### 1.5 包含错误的执行

```
╭──────────────────────────────────────────────────────────────╮
│ ● Read(src/app.tsx)                                          │
│   └ Read 89 lines from src/app.tsx                          │
│                                                              │
│ ● Bash(npm test)                                             │
│   └ Error: Command failed (exit 1)                           │
╰──────────────────────────────────────────────────────────────╯

⠧ Deciphering... (esc to interrupt · 23s · ↓ 512 tokens)
```

## 2. 颜色效果

### 2.1 工具状态颜色

```
┌─────────────────────────────────────────────────────────────┐
│ 状态      │ 图标 │ 颜色          │ 效果                     │
├───────────┼──────┼───────────────┼──────────────────────────┤
│ 等待      │  ○   │ textMuted     │ 灰色空心圆               │
│ 执行中    │  ⠋   │ textMuted     │ 灰色 spinner             │
│ 成功      │  ●   │ success       │ 绿色实心圆               │
│ 失败      │  ●   │ error         │ 红色实心圆               │
│ 取消      │  ○   │ textMuted     │ 灰色空心圆               │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 实际颜色值（Kanagawa Dark 主题）

| 元素 | 颜色键 | 十六进制 | 预览 |
|------|--------|----------|------|
| Spinner/状态词 | warning | #D7A657 | 🟡 |
| 工具名称 | primary | #A78BFA | 🟣 |
| 参数摘要 | textMuted | #727169 | ⚫ |
| 结果摘要 | text | #DCD7BA | ⚪ |
| 成功图标 | success | #98BB6C | 🟢 |
| 错误图标 | error | #E82424 | 🔴 |
| 思考标题 | secondary | #C4B5FD | 🟣 |
| 思考内容 | textMuted | #727169 | ⚫ |
| 边框（执行中） | warning | #D7A657 | 🟡 |
| 边框（完成） | border | #54546D | ⚫ |

## 3. 动画效果

### 3.1 Spinner 动画

```
dots 类型（推荐）：
⠋ → ⠙ → ⠹ → ⠸ → ⠼ → ⠴ → ⠦ → ⠧ → ⠇ → ⠏ → (循环)

间隔：80ms
```

### 3.2 状态短语轮换

```
Thinking... → Analyzing... → Processing... → Reasoning... →
Deciphering... → Elucidating... → Crunching... → Computing... → (循环)

间隔：3000-5000ms（随机）
```

### 3.3 Tip 提示轮换

```
"Press ctrl+t to show thinking process"
     ↓ (8s)
"Press esc to interrupt"
     ↓ (8s)
"Hit shift+tab to cycle modes"
     ↓ (8s)
(循环)
```

## 4. 响应式布局

### 4.1 窄终端（< 60 列）

```
╭────────────────────────────────╮
│ ● Read(src/theme.tsx)          │
│   └ Read 231 lines             │
│                                │
│ ⠋ Glob(**/*.tsx)               │
╰────────────────────────────────╯

⠙ Thinking... (32s)
  └ Press ctrl+t
```

### 4.2 宽终端（>= 80 列）

```
╭──────────────────────────────────────────────────────────────────────────────╮
│ ● Read(src/context/theme.tsx)                                                │
│   └ Read 231 lines from src/context/theme.tsx                               │
│                                                                              │
│ ● Glob(**/*.tsx)                                                             │
│   └ Found 26 files matching pattern                                          │
│                                                                              │
│ ⠋ Grep(useTheme)                                                             │
│   └ Searching...                                                             │
╰──────────────────────────────────────────────────────────────────────────────╯

⠙ Thinking... (esc to interrupt · 32s · ↓ 786 tokens)
  └ Tip: Press ctrl+t to show thinking process
```

## 5. 交互状态

### 5.1 快捷键提示

| 状态 | 提示内容 |
|------|----------|
| 思考中（收起） | `Tip: Press ctrl+t to show thinking process` |
| 思考中（展开） | 无额外提示 |
| 工具执行中 | `Tip: Press esc to interrupt` |
| 等待确认 | `Press y to confirm, n to cancel` |

### 5.2 状态转换动画

```
状态变化时的视觉反馈：

开始执行：
  [空白] → [Spinner 出现]

工具调用：
  [Spinner] → [工具行 + Spinner]

工具完成：
  [Spinner] → [●] (颜色变化)

执行完成：
  [所有内容] → [移入 Static 区域]
```

## 6. 错误状态展示

### 6.1 工具执行错误

```
╭──────────────────────────────────────────────────────────────╮
│ ● Read(src/app.tsx)                                          │
│   └ Read 89 lines from src/app.tsx                          │
│                                                              │
│ ● Bash(npm run build)                                        │
│   └ Error: Command failed with exit code 1                   │
│                                                              │
│     > npm ERR! code ELIFECYCLE                               │
│     > npm ERR! errno 1                                       │
╰──────────────────────────────────────────────────────────────╯
```

### 6.2 执行中断

```
○ Read(src/app.tsx)                ← 已取消（灰色）
  └ Cancelled by user

⚠ Execution cancelled
```

## 7. 最终效果对比

### Claude Code 风格

```
· Read(src/app.tsx)
  └ 89 lines

* Thinking... (esc to interrupt · 32s)
```

### Gemini CLI 风格

```
✓ Read src/app.tsx (89 lines)
o Glob **/*.tsx
? Shell npm test (confirm?)

⊷ Processing...
```

### 我们的风格（Reason CLI）

```
● Read(src/app.tsx)
  └ Read 89 lines from src/app.tsx

● Glob(**/*.tsx)
  └ Found 26 files matching pattern

⠋ Thinking... (esc to interrupt · 32s · ↓ 786 tokens)
  └ Tip: Press ctrl+t to show thinking process
```

**设计理念**：
- 简洁的圆点图标，通过颜色区分状态
- 两行结构（工具名 + 结果摘要）
- 完整的元信息（时间、Token）
- 友好的操作提示
